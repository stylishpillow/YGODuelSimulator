<ui:FluentWindow x:Class="YGODuelSimulator.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Icon="pack://application:,,,/Assets/app-icon.png"
        Title="Sign in — YGO Duel Simulator" Height="460" Width="420">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="YGO Duel Simulator"/>

        <StackPanel Grid.Row="1" Margin="32,4,32,28" VerticalAlignment="Center">
            <TextBlock Text="Welcome" FontSize="26" FontWeight="Bold"/>
            <TextBlock Text="Sign in, or create an account to continue."
                       Opacity="0.7" Margin="0,4,0,20"/>

            <TextBlock Text="Username" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <ui:TextBox x:Name="UsernameBox" PlaceholderText="username"/>

            <TextBlock Text="Password" FontWeight="SemiBold" Margin="0,14,0,4"/>
            <PasswordBox x:Name="PasswordBox" KeyDown="Password_KeyDown"/>

            <TextBlock x:Name="ErrorText" Foreground="#FF6B6B" TextWrapping="Wrap"
                       Margin="0,12,0,0" Visibility="Collapsed"/>

            <ui:Button Content="Log in" Appearance="Primary" HorizontalAlignment="Stretch"
                       Margin="0,20,0,0" Click="Login_Click"/>
            <ui:Button Content="Create account" HorizontalAlignment="Stretch"
                       Margin="0,8,0,0" Click="Register_Click"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
