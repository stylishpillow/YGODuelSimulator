<UserControl x:Class="YGODuelSimulator.Views.Controls.CardBrowser"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="340">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TextBox x:Name="SearchBox" Grid.Row="0" PlaceholderText="Search cards…"
                    Margin="0,0,0,8" TextChanged="SearchBox_TextChanged"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <ComboBox x:Name="CategoryFilter" Width="130" SelectionChanged="Filter_Changed"/>
            <ComboBox x:Name="AttributeFilter" Width="130" Margin="8,0,0,0" SelectionChanged="Filter_Changed"/>
        </StackPanel>

        <ListBox x:Name="ResultsList" Grid.Row="2"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 HorizontalContentAlignment="Stretch"
                 MouseDoubleClick="ResultsList_MouseDoubleClick">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Width="96" Margin="3" ToolTip="{Binding Name}">
                        <Image Source="{Binding Image}" Stretch="Uniform" Height="140"/>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
